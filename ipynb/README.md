# Machine Learning Models (ipynb)

This folder is used to store Jupyter Notebooks (`.ipynb` files) and trained ML models from Google Colab.

## Structure
- `/models`: Store exported model files (e.g., `.pkl`, `.h5`, `.pt`).
- `/notebooks`: Store the original Jupyter Notebooks.
